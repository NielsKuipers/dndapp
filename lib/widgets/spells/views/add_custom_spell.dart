import 'package:flutter/material.dart';

class AddCustomSpell extends StatelessWidget {
  const AddCustomSpell({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
